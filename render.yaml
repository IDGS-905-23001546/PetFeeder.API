services:
  - type: web
    name: petfeeder-api
    runtime: dotnet
    buildCommand: dotnet publish -c Release -o /app
    startCommand: dotnet PetFeeder.API.dll
    envVars:
      - key: SOMEE_CONNECTION_STRING
        sync: false
      - key: ASPNETCORE_URLS
        value: http://localhost:8080
